/** Static reference KPIs (pre-simulation baseline) — live dashboard uses `computeMetrics` + context */

export const dummyKpi = {
  supplyMld: 4.18,
  demandMld: 3.94,
  borewellPct: 87,
  shortageRiskPct: 18,
  tankerTrips: 14,
}
